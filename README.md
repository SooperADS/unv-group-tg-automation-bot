Python 3.14 (venv)

Libraries:

- `python-telegram-bot[all]`

Required files to start

- `token.txt` - telegram bot token
- `config/schedule.json` - schedule preset. Use `schemes/schedule.schema.json` as JSON schema for this file
- `logs` - this directory MUST exist? program doesn't create it automatically
